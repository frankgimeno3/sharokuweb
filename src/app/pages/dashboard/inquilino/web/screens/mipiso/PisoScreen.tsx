import React, { FC } from 'react';

interface PisoScreenProps {
  
}

const PisoScreen: FC<PisoScreenProps> = ({ }) => {
  return (
    <div className='p-12'>
    {/* <p>Welcome, {emailReceived} {typeReceived} </p>
    {pisoReceived != "vacio" && pisoReceived != "nulldefined" &&
      <MiPiso />
    }
    {pisoReceived == "vacio" &&
      <NoPiso />
    } */}

  </div>  );
};

export default PisoScreen;