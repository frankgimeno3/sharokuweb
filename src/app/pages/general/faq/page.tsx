import React, { FC } from 'react';

interface FAQProps {
  
}

const FAQ: FC<FAQProps> = ({ }) => {
  return (
    <div>
        <p>Preguntas frecuentes (FAQ)</p>
    </div>
  );
};

export default FAQ;