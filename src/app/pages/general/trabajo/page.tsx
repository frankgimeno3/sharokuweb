import React, { FC } from 'react';

interface TrabajoProps {
  
}

const Trabajo: FC<TrabajoProps> = ({ }) => {
  return (
    <div>
        <p>Trabaja con nosotros</p>
    </div>
  );
};

export default Trabajo;